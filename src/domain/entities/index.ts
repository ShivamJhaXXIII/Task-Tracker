/**
 * Barrel export file for domain entities
 * Similar to Java's interface or abstract class file that groups related domain objects
 *
 * Allows importing like this:
 * import { Task } from '@domain/entities'
 *
 * Instead of:
 * import { Task } from './Task'
 */
export { Task } from './Task';
