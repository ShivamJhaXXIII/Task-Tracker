/**
 * Barrel export file for domain services
 * Similar to Java's service class files
 *
 * Allows importing like this:
 * import { TaskDomainService } from '@domain/services'
 *
 * Instead of:
 * import { TaskDomainService } from './TaskDomainService'
 */
export { TaskDomainService } from './TaskDomainService';
